# Legacy per-role model config. .aidev/roles.json is AUTHORITATIVE (set-models.sh reads it first);
# this file only mirrors it for the legacy readers. Keep the two in step.
ARCHITECT_MODEL=claude-fable-5
ARCHITECT_EFFORT=high
PLANNER_MODEL=claude-opus-5
PLANNER_EFFORT=max
IMPLEMENTER_MODEL=claude-opus-5
IMPLEMENTER_EFFORT=medium
REVIEWER_MODEL=claude-opus-5
REVIEWER_EFFORT=high
